version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: DockerfileApp
    ports:
      - '4000:4000'
    volumes:
      - app:/app
    command: npm run dev
    networks:
      - network

  robotframework:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - robotframework:/test
    depends_on:
      - app
    networks:
      - network

networks:
  network:
    driver: bridge
    name: network

volumes:
  app:
  robotframework:
